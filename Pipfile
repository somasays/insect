[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
click = ">=8.0.0"
rich = ">=12.0.0"
gitpython = "*"
pytest = "*"

[dev-packages]
pytest = ">=7.0.0"
pytest-cov = ">=4.0.0"
black = ">=23.0.0"
isort = ">=5.0.0"
mypy = ">=1.0.0"
ruff = ">=0.0.230"
pre-commit = ">=3.0.0"
tox = ">=4.0.0"
toml = "*"
types-toml = "*"

[requires]
python_version = "3.8"
